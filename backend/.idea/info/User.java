public class User {
    String id;
    String password, name, email, sex; // 비밀번호, 이름, 이메일, 성별
    Integer focusSpan, restTime; // 집중 가능 시간(분), 휴식 시간(분)
    String preferredReminderTime, chronoType; // 선호 알림 시간, 생체 리듬 유형
}
